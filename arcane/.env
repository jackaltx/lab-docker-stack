# User and timezone
PUID=1000
PGID=1000
TZ=America/Chicago
DOMAIN=example.com
DOCKER_ROOT=/opt/Docker

# Arcane host CNAME for Traefik access
ARCANE_HOST=arcane

# Arcane configuration
# PORT=30258  # Old TrueNAS Scale app port - removed to use Arcane default (3552)
UMASK=002
GIN_MODE=release
ENVIRONMENT=production

# Secrets stored in: /mnt/zpool/Docker/Secrets/arcane.env
# (ENCRYPTION_KEY, JWT_SECRET)
