# Telegraf Configuration
PUID=568
PGID=568
TZ=America/Chicago
DOCKER_ROOT=/mnt/zpool/Docker

# InfluxDB v2 Output Settings
INFLUX_URL=http://monitor11.a0a0.org:8086
INFLUX_ORG=lavnet
INFLUX_BUCKET=telegraf

# Critical secrets stored in: /mnt/zpool/Docker/Secrets/telegraf.env
# (INFLUX_TOKEN - InfluxDB v2 authentication token)

# Telegraf Agent Settings
TELEGRAF_HOSTNAME=truenas.a0a0.org
TELEGRAF_INTERVAL=60s
