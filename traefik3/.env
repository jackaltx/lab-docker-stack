# Traefik configuration

# Dashboard basic auth password (bcrypt hash)
# SECURITY NOTE: This hash MUST stay in git-tracked .env due to Docker Compose limitation
# - Labels require ${USER_PASSWD} substitution at compose parse-time
# - env_file: loads variables into container AFTER parsing
# - Bcrypt hash is relatively safe to expose (computationally expensive to crack)
# TODO: Move to Traefik config file or Ansible template for better security
USER_PASSWD=lavender:$$apr1$$f.DJH4dC$$BtWCj60DOrDaiGxjWy8pi0

# Critical secrets stored in: /mnt/zpool/Docker/Secrets/traefik.env
# (LINODE_TOKEN - controls DNS for entire domain)

# To regenerate USER_PASSWD hash:
# echo $(htpasswd -nb lavender NEW_PASSWORD) | sed -e s/\\$/\\$\\$/g