# Claude Code configuration (except commands)
.claude/*
!.claude/commands/

# Local environment overrides (if created)
**/.env.local
**/.env.secrets

# Note: Standard .env files ARE tracked (contain PUID/PGID/TZ only)
# Secrets stored separately in: /mnt/zpool/Docker/Secrets/


# Protect .env files from commits - DISABLED (we want templates in git)
# **/.env
# *.env
# Use .env.local or .env.secrets for actual secrets (these ARE gitignored)

# Protect .env files from commits (added by sync-env.sh -p)
# **/.env
